# Promotion Decisions

The current `agent-team-work` route contract is retained. The route scorecard has 8/8 correct cases with no misroutes or ambiguous cases, and no candidate description has outperformed the current contract.

This is a route-contract decision only. It does not authorize an unconditional public production release. Provider-backed holdout execution, human adjudication, native enforcement, client telemetry, accepted world-class evidence, and the final benchmark release lock remain separate gates.

- Decision: `keep_current`
- Route evidence: `reports/route_scorecard.json`
- Public release decision: `blocked_pending_external_and_human_evidence`
- Full status: `reports/benchmark_reproducibility.json`
