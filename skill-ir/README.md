# Skill IR compatibility files

`schema.json` is the host Skill IR schema reference. `examples/yao-meta-skill.json` is the legacy path expected by the governed audit; its payload is the current `agent-team-work` export from `reports/skill-ir.json`. The report remains the authoritative generated evidence.
